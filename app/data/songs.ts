import type { Song } from '../types/music';

// Legacy function - now returns empty array since we use context
export function getUploadedSongs(): Song[] {
  return [];
}

// Legacy function - now returns empty array since we use context
export function getAllSongs(): Song[] {
  return [];
}
