"use client";

// YouTube search removed per request. Keep a null stub to avoid import errors
// in places that might still reference this component.

export default function YouTubeSearch() {
  return null;
}