import { NextResponse } from 'next/server';

export async function GET() {
	// Placeholder route to satisfy TypeScript module checks.
	// Not implemented — return 404 for now.
	return new NextResponse('Not implemented', { status: 404 });
}
